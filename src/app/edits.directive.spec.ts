import { EditsDirective } from './edits.directive';

describe('EditsDirective', () => {
  it('should create an instance', () => {
    const directive = new EditsDirective();
    expect(directive).toBeTruthy();
  });
});
